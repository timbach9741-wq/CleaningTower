fix: granular error handling for notification permission and FCM token

Rewrote requestNotificationPermission and handleToggleNotification
to handle each failure point individually instead of one big try/catch.
No more generic scary error popup on any browser.
